---
title: Installation
seq: 1
---

extends /layouts/_docs

block docs-content
  section
    :marked
      ### Requirements

      * ZSH 4.3.12+
      * The [color](https://github.com/molovo/color) and [revolver](https://github.com/molovo/revolver) executables. The Zulu installation method will install these for you, or you can install them yourself using the commands to the right.

  feature.bump
    :marked
      ```bash
      curl -L https://raw.githubusercontent.com/molovo/revolver/master/revolver > /usr/local/bin/revolver
      curl -L https://raw.githubusercontent.com/molovo/color/master/color.zsh > /usr/local/bin/color
      ```

  section
    :marked
      ### Installing with [Zulu](https://zulu.sh) (recommended)

      Installing with Zulu will handle building ZUnit from source, and installing all dependencies for you.

  feature.bump
    :marked
      ```bash
      zulu install zunit
      ```

  section
    :marked
      ### Installing Manually

      To install manually, just clone the ZUnit source code and build the executable. Once done, copy the `zunit` executable to somewhere in your `$path`.

  feature.bump
    :marked
      ```bash
      git clone https://github.com/molovo/zunit
      cd zunit
      ./build.zsh
      cp zunit /usr/local/bin
      ```
